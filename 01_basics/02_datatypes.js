"use strict"; //treat all JS code as newer version as it is also not needed because machine automatically treats it as new version of JS
// alert(3 + 3); // We are usimg NodeJS, not browser

console.log(3 + 3);

//code readability should be high write code in such a way that it should be easily readable and understanable.

console.log("Aniruddh")



let name = "Aniruddh"
let age = 18
let isloggedIn = true
let state;


// number ==> 2 to the power 53 also we have Bigint
// string ==> ""
// boolean ==> true/false
// null ==> stand alone value, a type, can be assigned as null
// undefined ==> no value has been assigned, is a type itself
//symbol ==> to find uniqeness between objects and all

//object 

console.log(typeof null); //type : object
console.log(typeof undefined); //type : undefined
